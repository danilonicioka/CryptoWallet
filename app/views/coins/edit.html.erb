<h1><%= "#{t('editing')} #{Coin.model_name.human}" %></h1>

<%= render 'form', coin: @coin %>

<%= link_to t('links.show'), @coin, class:"btn btn-info" %> |
<%= link_to t('links.back'), coins_path, class:"btn btn-secondary" %>
