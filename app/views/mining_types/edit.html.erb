<h1><%= "#{t('editing')} #{MiningType.model_name.human}" %></h1>

<%= render 'form', mining_type: @mining_type %>

<%= link_to t('links.show'), @mining_type, class:"btn btn-info" %> |
<%= link_to t('links.back'), mining_types_path, class:"btn btn-secondary" %>
